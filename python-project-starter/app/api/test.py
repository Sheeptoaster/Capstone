from random import random
from decimal import Decimal

from flask import jsonify

# change = Decimal(random())

# price = Decimal(100)
# weight = Decimal(0.5)
# print("CHANGE", change)

# if weight < change:
#     print("PRICE U", price * (change * Decimal(3.5) / Decimal(100)) + price)
#     print("WEIGHT", weight + (weight * (change / Decimal(8.5))))
# else:
#     print("PRICE D", price * (change * Decimal(3.5) / Decimal(100) * Decimal(-1)) + price)
#     print("WEIGHT", weight - (weight * (change / Decimal(13.5))))

# const roomId = useParams()
# let view = <></>

# useEffect(async() => {
#     const user = useState(state => state.session.user)
#         if (user) {
#             dispatch(getRooms(user.id))
#             const rooms = useState(state => state.user.rooms)
#             if rooms.id === user.id {
#                 view = <Room Component />
#             } else {
#                 view = <Home Component />
#             }
#         } else {
#             view = <></>
#         }
# }, [roomId])
